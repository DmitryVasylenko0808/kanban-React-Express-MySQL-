const config = {
    db: {
        host: 'localhost',
        user: 'root',
        database: 'kanban',
        password: 'mysql'
    },
    PORT: 3000,
    secret_key: 'secret222'
}

module.exports = config;